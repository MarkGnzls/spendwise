services:
  - type: web
    name: spendwise
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn run:app
    envVars:
      - key: SECRET_KEY
        value: changeme
databases:
  - name: spendwise-db
    plan: free
